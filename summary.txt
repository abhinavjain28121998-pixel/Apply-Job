I have completely overhauled the end-to-end Demo Mode workflow to ensure strict data consistency and a realistic user experience:

- **Centralized Application Readiness**: Created `applicationReadinessService` to serve as the single source of truth for Readiness calculation. It provides a real numeric score (0-100) and actionable reasons (e.g., "Resume not optimized", "Cover letter missing"). The UI now relies on this service exclusively.
- **Job Search Relevance**: Replaced the generic mock data with a tailored dataset of 10 realistic Digital Marketing jobs. The `MockJobProvider.searchJobs()` now features genuine keyword filtering—searching "digital marketing" explicitly scans job titles, descriptions, and roles to return only relevant matches.
- **Accurate Provider Status**: Refined the application header. It now explicitly reads **Demo Mode &middot; Mock Jobs** for local sessions and **Live Provider &middot; Connected** for configured production environments, eliminating contradictory statuses.
- **Application Workspace Fidelity**: Rebuilt the Workspace layout. It now safely handles jobs that are "Not Analyzed" (displaying a clear call-to-action instead of a misleading 0%). When analyzed, it persists the `JobMatch` record into `jobService` (and localStorage) so Match Score, tailored CVs, and generated answers survive full page reloads.
- **Realistic Demo Profile**: `resumeService` now provisions a comprehensive, realistic Digital Marketing demo profile for new local sessions. All downstream Gemini analyses now grade the mock jobs against this exact profile.

You can now test the complete `Search → Save → Analyze → Workspace → Tailor` flow!
